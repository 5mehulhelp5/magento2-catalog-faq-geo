<?php
/**
 * Magendoo Faq Ask a Question Form Template
 *
 * @category  Magendoo
 * @package   Magendoo_Faq
 * @copyright Copyright (c) Magendoo (https://magendoo.com)
 * @license   https://opensource.org/licenses/OSL-3.0 Open Software License v. 3.0 (OSL-3.0)
 */

/** @var \Magendoo\Faq\Block\Faq\Question\AskForm $block */
?>
<div class="faq-ask-form-wrapper">
    <h3 class="faq-ask-form-title"><?= $escaper->escapeHtml(__('Ask a Question')) ?></h3>
    <form action="<?= $escaper->escapeUrl($block->getSubmitUrl()) ?>"
          method="post" id="faq-ask-form" class="form faq-ask-form"
          data-hasrequired="<?= $escaper->escapeHtmlAttr(__('* Required Fields')) ?>">
        <?= $block->getBlockHtml('formkey') ?>
        <?php if ($block->getCurrentProductId()): ?>
            <input type="hidden" name="product_id" value="<?= (int)$block->getCurrentProductId() ?>"/>
        <?php endif; ?>
        <fieldset class="fieldset">
            <div class="field required">
                <label class="label" for="faq-ask-name">
                    <span><?= $escaper->escapeHtml(__('Your Name')) ?></span>
                </label>
                <div class="control">
                    <input type="text" id="faq-ask-name" name="sender_name"
                           class="input-text" required
                           value="<?= $escaper->escapeHtmlAttr($block->getLoggedInCustomerName()) ?>"/>
                </div>
            </div>
            <div class="field required">
                <label class="label" for="faq-ask-email">
                    <span><?= $escaper->escapeHtml(__('Email')) ?></span>
                </label>
                <div class="control">
                    <input type="email" id="faq-ask-email" name="sender_email"
                           class="input-text" required
                           value="<?= $escaper->escapeHtmlAttr($block->getLoggedInCustomerEmail()) ?>"/>
                </div>
            </div>
            <div class="field required">
                <label class="label" for="faq-ask-title">
                    <span><?= $escaper->escapeHtml(__('Your Question')) ?></span>
                </label>
                <div class="control">
                    <textarea id="faq-ask-title" name="title"
                              class="input-text" rows="4" required></textarea>
                </div>
            </div>
            <?php if ($block->isGdprEnabled()): ?>
            <div class="field required faq-gdpr-consent">
                <div class="control">
                    <input type="checkbox" id="faq-gdpr" name="gdpr_consent" value="1" required
                           class="checkbox"/>
                    <label class="label" for="faq-gdpr">
                        <span><?= $escaper->escapeHtml($block->getGdprText(), ['a', 'strong', 'em', 'br']) ?></span>
                    </label>
                </div>
            </div>
            <?php endif; ?>
        </fieldset>
        <?= $block->getChildHtml('form_additional_info') ?>
        <div class="actions-toolbar">
            <div class="primary">
                <button type="submit" class="action submit primary" title="<?= $escaper->escapeHtmlAttr(__('Submit Question')) ?>">
                    <span><?= $escaper->escapeHtml(__('Submit Question')) ?></span>
                </button>
            </div>
        </div>
    </form>
</div>
